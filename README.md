# Img64Transfer

⚠️ This project is currently under development and is not yet ready for production use.

An internet-free image sharing application that streams images through structured, animated QR code loops for secure offline transfer.
- `npm install` to download the dependencies.
- `npm test` to run the defined tests.
- `npx electron ./main.js` or `npx start` to run the app.

Icons by [Icons8](https://icons8.com)